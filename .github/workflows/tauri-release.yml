# Archive: former Tauri release workflow
# Native releases now use React Native local builds (see README).
# Kept for reference only — disabled.
name: (archived) Tauri release

on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Tauri releases archived. Use npm run android / ios / windows / macos locally."
